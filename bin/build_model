#!/bin/bash
#
# Building the flow model from source
cd ./source/
make
cd ..
ls -la
#
#
# runnning a test case - can't get it to work in travis
#mv ../APM-MODEL.EXE .
#./APM-MODEL.EXE fixtures/PARALELL-PLATE-01VOX_INIT.INP
